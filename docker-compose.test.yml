version: "3"

services:
  flux:
    build: .
    environment:
      VIRTUAL_HOST: test.flux.camino.beta.gouv.fr
      VIRTUAL_PORT: ${NODE_PORT}
      NODE_PORT: ${NODE_PORT}
    expose:
      - ${NODE_PORT}

networks:
  default:
    external:
      name: nginx-proxy
